Reverse the operation or condition in the original instruction to test the opposite behavior.

For example:

Original: Test that values with the drop ability are automatically dropped at the end of scope.
New: Test that values without the drop ability cannot be automatically dropped and must be explicitly consumed.

Original: Test successful function calls with valid parameters.
New: Test failed function calls with invalid parameters.