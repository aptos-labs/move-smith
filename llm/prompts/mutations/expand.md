Expand the original instruction with more details and concrete concepts.

For example:

Original: Test copyable values can be used multiple times.
New: Test that local variables can be copied and used multiple times within a function for both primitive types and structs with the copy ability.

Original: Include `friend` declarations in the module output.
New: Create a function with the `friend` specifier and call this function from another module.
