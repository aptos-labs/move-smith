**Error:** `from a function which returns nothing`

Functions with no return type cannot end with expression.

If the functin intends to return something, specify return type like `fun my_function(): u64 { ...; x }`
